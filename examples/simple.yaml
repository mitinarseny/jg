files:
  file1:

root:
  bool: bool
  boolExplicit:
    type: bool

  integerInline: integer # default range: [0, 100]
  integerRange:
    type: integer
    range: [0, 100]
  integerChoices:
    type: integer
    choices:
      - 2
      - 3
      - 5
      - 7

  float: float # default range: [0,1)
  floatRange:
    type: float
    range: [0, 1]
  floatChoices:
    type: float
    choices:
      - 3.14
      - 2.71

  stringFrom:
    type: string
    from: file1
  stringChoices:
    type: string
    choices:
      - choice1
      - choice2
      - choice3

  array:
    type: array
    length: 100 # default: [0, 10]
    elements: integer

  object:
    type: object
    fields:
      field1: integer
